Project TODOs

- [ ] Nutrition feature completion (assigned to groupmate)
  - finalize `nutrition_logs` service/routes/templates end-to-end
  - ensure create/update/delete/list flows use stored procedures consistently
  - add any missing schema models + form validation

- [ ] Workout feature completion (assigned to groupmate)
  - finalize `workout_logs` service/routes/templates end-to-end
  - ensure create/update/delete/list flows use stored procedures consistently
  - add any missing schema models + form validation

- [ ] Goal feature completion
  - implement `goal_service` methods for create/update/status/list using goal procedures
  - add `goals` router endpoints + forms (create, edit, pause/complete)
  - add `goal.py` schema models with enum/date validation and owner-safe updates
  - add goals UI page and link from nav/dashboard

- [ ] Metrics feature completion
  - implement `metric_service` upsert/list methods for `daily_metrics`
  - add `metrics` router with submit flow + recent metrics table
  - add `metric.py` schema models for weight/steps/sleep/water validation
  - ensure one-entry-per-day UX matches DB unique constraint

- [ ] Dashboard functions and page
  - implement `dashboard_service` aggregation helpers (recent check-ins, goals, achievements, groups)
  - update `dashboard.html` to show useful cards/quick stats instead of placeholders
  - add graceful empty/error states if some modules are unfinished
  - keep dashboard queries lightweight (recent N rows, not full-table scans)

- [ ] Achievement checking functions (rule evaluation)
  - add app-side rule checks (e.g., steps streak, hydration streak, yoga sessions)
  - trigger checks after relevant writes (metrics/check-ins/workouts/nutrition)
  - grant idempotently via `sp_grant_user_achievement_by_code`
  - document rule definitions so expected behavior is testable